import chai, { expect } from 'chai';
import sinon from 'sinon'
import sinonChai from 'sinon-chai';


chai.use(sinonChai);

import EventService from '../../src/services/event.services.js';

var sandbox


describe('habit service tests', () => {
    context('getHabits', () => {
        it('should return all habits', async () => {

        });
    })
})
