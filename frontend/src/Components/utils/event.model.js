export default class EventModel {
    constructor(name, minutes, distance, date, habitId) {
        this.name = name;
        this.minutes = minutes;
        this.distance = distance;
        this.date = date;
        this.habitId = habitId;
    }
}
